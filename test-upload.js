import fs from 'fs'
import fetch from 'node-fetch'
import FormData from 'form-data'

async function run() {
  console.log('Test file created. Backend testing can be done with Postman or similar to avoid browser DOM issues.')
}
run()
